const startInterviewButton = document.querySelector("#startInterviewButton");

startInterviewButton?.addEventListener("click", () => {
  window.alert(
    "Task 1 demo: Your AI mock interview landing page is ready. Next task: connect domain selection and interview setup.",
  );
});
